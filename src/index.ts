// OpenSafari MCP Server — Main Entry Point
// iOS Safari automation via Xcode Simulator + WebKit Remote Debugging Protocol

// Note: console.error is used intentionally — MCP stdio transport breaks if console.log is used
console.error('OpenSafari MCP server starting...');
